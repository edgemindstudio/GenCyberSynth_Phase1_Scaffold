# Paper 05 — Robustness & Calibration Under Distribution Shift

Goal: shift benchmarks + calibrated baselines + reliability analysis.

Reproduce:
1) Create shifts: <command>
2) Train baseline + calibrate: <command>
3) Evaluate under shift: <command>

Outputs:
- artifacts/paper05/... (see paths)
