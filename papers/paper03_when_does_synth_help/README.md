# Paper 03 — When Does Synthetic Data Help Malware Classification?

Goal: utility-first ablations (synthetic ratios) + minority metrics + calibration.

Reproduce:
1) Generate synthetic at ratios: <command>
2) Train downstream models: <command>
3) Evaluate utility + calibration: <command>

Outputs:
- artifacts/paper03/... (see paths)
