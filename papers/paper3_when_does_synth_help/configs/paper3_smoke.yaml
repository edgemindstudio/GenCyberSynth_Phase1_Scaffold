run_meta:
  paper_id: paper3
  config_id: paper3_smoke
  dataset_id: ustc_tfc2016

paths:
  artifacts: artifacts   # overridden by --artifacts

# Training dataset directory used by gan/train.py style trainers
DATA_DIR: /home/bruno.fonkeng/gencys/data/USTC-TFC2016_malware_nhwc

# smoke training (keep tiny)
train:
  epochs: 2
  batch_size: 64

# smoke synth
synth:
  seed: 42
  n_per_class: 10

# smoke eval
evaluator:
  per_class_cap: 50